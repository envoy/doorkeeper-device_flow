require 'spec_helper'

describe Doorkeeper::DeviceFlow do
  it 'has a version number' do
    expect(Doorkeeper::DeviceFlow::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
